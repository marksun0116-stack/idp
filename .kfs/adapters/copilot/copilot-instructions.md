# GitHub Copilot — KFS

Follow **`.kfs/INDEX.md`**. Before editing `knowledge/` read `.kfs/agent/runbooks/merge-knowledge-first.md`; before `src/` read `implement-with-primitives.md`.

Truth: `knowledge/catalog.yml`. Do not bulk-load docs outside `.kfs/` unless asked.

Install: `./.kfs/install.sh --tool copilot`
