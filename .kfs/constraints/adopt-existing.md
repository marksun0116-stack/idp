# Adopt existing application (RULE-013 baseline)

- **Default adoption** for running apps: reverse-engineer **`docs/`** + **`knowledge/`** from **`src/`**, **`tests/`**, optional **`references/`**
- **No current/target split** — one baseline; then same flow as greenfield
- **Major modernization** → **new repo** ([bootstrap-modernization-repo.md](../agent/runbooks/bootstrap-modernization-repo.md)); legacy keeps as-is baseline only
- Spec: `.kfs/specs/adopt_existing_repo_spec.md`
